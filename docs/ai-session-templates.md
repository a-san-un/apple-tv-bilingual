# AI Session Templates

このファイルは、Perplexity（このチャット）と VS Code の GitHub Copilot / Serena に渡すための、AI セッション運用ルールと再利用テンプレートをまとめたものです。

この文書で扱うもの:

- ユーザー → Perplexity に渡す「次スレ冒頭テンプレ」
- Perplexity がセッション開始時に確認するチェックリスト
- Perplexity → VS Code の GitHub Copilot / Serena に渡す指示文テンプレ
- docs / 実装 / ターミナル運用 / commit 運用の共通ルール

この文書の目的は、毎回同じ前提説明を繰り返さず、現在位置・役割分担・安全策・完了条件を短く正確に共有できるようにすることです。[cite:800]

---

## 1. セッション運用方針

### 1.1 役割分担

- Perplexity（このチャット）の役割:
  - 対象リポジトリ / ブランチ / docs を読み、現在位置の整理を担当する
  - issue / Phase / roadmap / 分割方針をもとに、タスクの切り分けと進め方の整理を行う
  - VS Code 側の GitHub Copilot / Serena に渡すための、調査 / 実装 / docs 更新用の指示文を組み立てる
  - 「変更しない範囲」「完了条件」「不要コード削除・重複回避」などの安全策を明文化する
  - docs を先に整理したい場合は、このチャット側で文案を直接整える

- VS Code の GitHub Copilot / Serena の役割:
  - このチャットから渡された指示文に従って、コード調査 / 実装 / docs 反映 / コミット準備を担当する
  - 2 ファイル以上に波及する変更や、責務境界の追跡が必要な場合は Serena MCP を使って影響範囲確認・差分生成・不要コード整理を行う
  - 軽微な 1 ファイル修正や、要件が十分固まっている変更は通常の Copilot 提案で対応する

### 1.2 Serena を使う基準

- 基本ルールは **「2 ファイル以上に波及しそうなら Serena、1 ファイルだけの軽微修正なら通常の Copilot」**
- ただし 1 ファイルでも、影響範囲が読みにくい場合、参照関係を正確に追いたい場合、既存ルールとの整合確認が重要な場合は Serena を使ってよい
- Serena は「判断コストが高い作業」で優先して使う
- Perplexity は調査・切り分け・方針整理・プロンプト作成を担当し、VS Code の Copilot / Serena は実際の修正・反映・コミット準備を担当する

### 1.3 docs の運用方針

- docs は差分修正前提ではなく、**必要ならフルアップデートしてよい**
- `dev-roadmap` / `contentjs-split-roadmap` / `atv-design` / `ai-session-templates` は、その時点で読んで全体像が分かる状態を優先する
- 古い章立てや重複説明を無理に残さず、必要なら章ごと整理し直す
- ただし docs ごとの役割は崩さない
- docs 更新は、できるだけこのチャット側で文案を先に整えてから VS Code 側へ渡す

### 1.4 分割作業の前提

- `content.js` の分割は、既存挙動をできるだけ変えずに責務を分けるために行う
- 同時に、コード量を減らし、見通しを良くし、追跡しやすくすることも重要な目的とする
- 一括分割ではなく、Phase 単位で安全に段階分割する
- 純関数・独立責務を先に切り出し、DOM 依存・observer 依存・Apple TV+ 固有 UI 依存が強い層は後ろに回す
- 全面分割に一気に進まず、roadmap に沿って現在の責務境界を明確化しながら薄くしていく

### 1.5 VS Code ターミナル運用

- 分割作業や調査は、VS Code ターミナルの `git` / `grep` / `sed` / `node` / `python3` などを使って進める
- 例: `git status` / `git diff --stat` / `grep -n` / `sed -n` / `node --check` を使って、影響範囲確認・抜粋確認・構文確認を行う
- 確認コマンドや更新処理が長くなる場合は、`python3 - <<'PY'` 形式の小さなスクリプトにまとめて実行してよい
- 長い確認コマンドは必要に応じて分割し、確認コマンド群を先にまとめて出し、その後で更新コマンドを実行する
- ターミナルでの確認は、修正前の現状確認、修正後の構文確認、差分確認まで一連で行う

### 1.6 基本ルール

- セッション開始時に、対象リポジトリ / ブランチ / 関連 docs / 関連 issue を明示する
- `docs/dev-roadmap.md` と `docs/contentjs-split-roadmap.md` を必要な範囲で読み、現在位置を把握してから作業に入る
- すでに完了している範囲と、今回戻らない範囲（再調査・再実装しないもの）を整理してから進める
- 指示文では「目的」「対象」「変更しない範囲」「完了条件」を明示する
- 変更は最小差分を基本とするが、docs は必要に応じてフルアップデートしてよい
- 既存 helper / 既存フロー / 既存 state を再利用し、別経路のコピペ実装を増やさない
- 同じ責務の処理を分岐ごとに複製しない
- 旧ロジックを残したまま新ロジックを継ぎ足さず、置き換えられるものは置き換える
- ただし削除は「確実に不要」と判断できるものだけに限定し、不確実なものは残して報告する
- 置き換え後に不要になった旧分岐・未使用変数・古い説明文は削除候補として確認する
- commit / push が含まれる場合は、指示文の早い段階で「今回は commit まで」「今回は push まで」と明示する
- 作業完了後は、必要に応じて完了 / 未完了が分かる進捗表や状態リストを更新する

### 1.7 docs の役割

- `docs/dev-roadmap.md`
  - issue / phase / 状態 / 優先順位 / 次バッチの実装順を書く

- `docs/contentjs-split-roadmap.md`
  - `content.js` 分割方針、Phase A〜E の段階順、責務境界、次に薄くしていく対象を書く

- `docs/atv-design.md`
  - UI 仕様、primary / secondary 描画パターン、責務境界、表示ルールを書く

- `docs/ai-session-templates.md`
  - AI への依頼テンプレ、役割分担、セッション進行ルール、ターミナル運用、調査 / 実装 / docs 更新テンプレを書く
  - 個別 issue の詳細仕様は書き込みすぎず、再利用可能な形に保つ

---

## 2. このチャット用チェックリスト

セッション開始時に、このチャットが確認すること:

- 対象リポジトリ / ブランチ / 関連 Issue / Phase を確認したか
- `docs/dev-roadmap.md` と `docs/contentjs-split-roadmap.md` を必要な範囲で読み、現在位置を把握したか
- `docs/atv-design.md` を読むべきテーマかどうか判断したか
- すでに完了している範囲と、今回戻らない範囲を整理したか
- 今回の目的を分類したか
  - 調査 / 実装 / docs 整備 / commit / push だけ など
- 触らない責務を把握しているか
  - 例: resolver / secondaryLang fallback / 全面分割しない前提 / 現フェーズ外の observer 調整 など
- 既存処理へ寄せるべきか、新規追加が本当に必要かを切り分けたか
- 重複コードを増やさないルールを意識しているか
- 今回の変更で不要コード・旧分岐・未使用変数・死んだ説明文が発生しないか意識しているか
- Serena MCP を使う条件に当てはまるか確認したか
  - 2 ファイル以上に波及
  - 影響範囲が読みにくい
  - 既存ルールとの整合確認が重要
- 条件に当てはまる場合、下のテンプレから適切なものを選んで Copilot / Serena 用指示文を作るべきか
- docs 更新時に、何を `dev-roadmap` に書き、何を `contentjs-split-roadmap` や `atv-design` に書くべきか切り分けたか
- docs を差分ではなくフルアップデートした方が読みやすいか判断したか
- 必要な確認コマンドを VS Code ターミナルでどう打つか、または Python でまとめるべきか考えたか

---

## 3. 次スレ冒頭テンプレ（ユーザー → Perplexity）

```text
今回のセッションの前提:

- 対象リポジトリ:
- 対象ブランチ:
- 関連する Issue / Phase:
- すでに完了しているタスク:
  -
  -
- 今回は戻らない範囲（再調査・再実装しないもの）:
  -
  -

今回の目的:
-

今回このスレでやりたいこと:
1.
2.
3.

このセッションの終了条件:
-

今回のルール:
- docs は必要ならフルアップデートしてよい
- content.js は roadmap に沿って段階分割し、全面分割に一気に進まない
- resolver / secondaryLang fallback の仕様は変更しない
- 重複コードを増やさない（共通 helper / 既存フローを優先）
- 不要コードを残さない（置き換え後の旧分岐・未使用変数・古い文言は削除候補として確認）
- commit / push がある場合は最初に明示する
```

---

## 4. 共通ルール（実装・docs 共通）

- 変更は目的に対して最小限に保つ
- docs は必要なら差分修正ではなくフルアップデートしてよい
- 既存 helper / 既存フロー / 既存 state を再利用し、別経路のコピペ実装を増やさない
- 同じ責務の処理を分岐ごとに複製しない
- 今回の issue の責務を超える全面リファクタリングは行わない
- 実装でも docs でも、変更によって不要になった旧記述・旧分岐・未使用変数・死んだ説明は削除候補として確認する
- 旧ロジックを残したまま新ロジックを追加する形は避け、置き換えられるものは置き換える
- ただし削除は「確実に不要」と判断できるものだけに限定し、不確実なものは残して報告する
- 既存コメントは壊さず、責務境界を補う短いコメントを必要箇所へ追加する
- docs の粒度は役割で分ける
  - 実装順 / 優先順位 / phase 管理 → `docs/dev-roadmap.md`
  - 分割順 / 責務境界 / 今後の薄くし方 → `docs/contentjs-split-roadmap.md`
  - 画面仕様 / UI ルール / 描画パターン / 責務境界 → `docs/atv-design.md`
  - AI への渡し方 / テンプレ / セッション進行ルール / ターミナル運用 → `docs/ai-session-templates.md`
- commit / push を含む場合は、指示文の早い段階で「今回は commit まで」「今回は push まで」と明示する
- code と docs を同時に変更した場合は、必要に応じて commit を分ける
- commit 前には `git status` と `git diff --stat` を確認し、必要ならファイルごとの差分も見る

---

## 5. 調査テンプレート（Perplexity → Copilot / Serena）

```text
VS Code の GitHub Copilot / Serena MCP でこのプロジェクトを有効化してください。
プロジェクト名:
対象ブランチ:

今回は調査のみです。コード変更はまだしないでください。

現在位置:
- Phase:
- Issue:
- ここまで完了していること:

目的:
-

確認対象:
-

見てほしい観点:
-
-
-

変更しない範囲:
-
-

ターミナル確認の方針:
- 必要な確認は VS Code ターミナルで行う
- `grep -n` / `sed -n` / `git diff` などで根拠を確認してから回答する
- 長い確認手順は必要に応じて分割してよい

不要コード削除・重複回避の観点:
- 今回の対象範囲で、不要になっている旧ロジックや未使用変数がないか
- 同じ責務の処理が別経路に重複していないか
- 新規 helper を足さずに既存 helper / 既存フローへ寄せられる余地があるか

出力してほしい内容:
- 現状整理
- 問題点
- 最小変更案
- 影響範囲
- 不要コードや重複の懸念箇所
```

---

## 6. 実装テンプレート（Perplexity → Copilot / Serena）

```text
VS Code の GitHub Copilot / Serena MCP でこのプロジェクトを有効化してください。
プロジェクト名:
対象ブランチ:

今回は実装まで進めてください。

現在位置:
- Phase:
- Issue:
- ここまで完了していること:
  -
  -

目的:
-

対象ファイル:
-

要件:
-
-
-

変更しない範囲:
- 今回は触らない責務:
  -
- 仕様を変えない部分:
  -
- roadmap / docs の記述と矛盾させないこと

VS Code ターミナルでの確認・更新方針:
- 修正前に `grep -n` / `sed -n` / `git status` などで対象範囲を確認する
- 修正後に `node --check`、必要に応じて `git diff -- <file>` で差分確認する
- 複雑な置換や更新は `python3 - <<'PY'` の小さなスクリプトでまとめて実行してよい
- 可能なら確認コマンド群を先にまとめ、その後で更新コマンドを実行する

不要コード削除・重複回避（毎回必須）:
- 今回の変更で不要になった旧分岐・未使用変数・死んだ処理があれば削除する
- 既存 helper / 既存フロー / 既存 state に寄せられるなら寄せる
- 同じ責務の処理を別経路に複製しない
- 旧ロジックを残したまま新ロジックを継ぎ足さない
- ただし、確実に不要と判断できるものだけ削除する

commit / push 方針:
- 今回は commit まで / push まで
- どの単位で commit するか:
  -

完了条件:
- 変更ファイル一覧
- 変更内容
- 理由
- テスト観点
- 削除した不要コード
- 重複回避のために整理した点
- 必要なら docs / issue 更新案
```

---

## 7. docs 更新テンプレート（Perplexity → Copilot / Serena）

```text
VS Code の GitHub Copilot / Serena MCP でこのプロジェクトを有効化してください。
プロジェクト名:
対象ブランチ:

今回は docs 更新のみです。実装コードは変更しません。

目的:
-

対象ファイル:
- docs/...

やってほしいこと:
1.
2.

制約:
- 必要なら差分更新ではなくフルアップデートしてよい
- docs ごとの役割を崩さない
- トーンは既存 docs に合わせる
- 内容が最新実装と矛盾していないか確認してから書き換える
- 重複説明は減らすが、必要な背景説明までは削らない

不要記述削除・重複回避（毎回必須）:
- 実装変更で古くなった説明があれば削除または更新する
- 同じ説明を複数ファイルに重複して書きすぎない
- docs ごとの役割に合わせて粒度を分ける
- 章立てごと整理し直した方が読みやすい場合は、全面的に組み替えてよい

完了時の報告:
- 変更ファイル一覧
- 各ファイルで何を変えたか
- 削除した古い記述や重複記述
- 変更不要と判断したファイル
```

---

## 8. 使い分けの目安

### Serena を使うべき場面

- 2 ファイル以上に波及しそうな変更
- 影響範囲が読みにくい変更
- 呼び出し関係を正確に追いたい調査
- 既存ルールや過去判断に合わせたい実装
- 修正理由や検証内容まで整理して残したいとき

### Serena を使わなくてもよい場面

- 単純な 1 ファイル軽微修正
- 影響範囲が明白な見た目だけの小修正
- すでに修正方針が十分固まっている置換作業

### 例外

- 1 ファイルだけでも、参照関係確認が必要、既存ルール整合が重要、説明責任が重い場合は Serena を使ってよい
